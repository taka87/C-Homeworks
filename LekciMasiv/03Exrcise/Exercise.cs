using System;

class Exercise
{
    static void Main()
    {
        string[] days = { "monday", "saturday", "drugday" };

        Console.WriteLine(string.Join(", ", days));  // radelya dnite bez for cikal s zapetaika eidn sled drug v masiva
    }
    //result.Trim -> maha shpaciite ot dvete strani na masiva
}
